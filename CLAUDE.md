# Claude Code — uiwrap

Use **`AGENTS.md`** in this directory as the single source of truth for build commands, tests, layout, and safety rules for the **uiwrap** library.

Claude-specific: prefer small, reviewable diffs; do not add repo-wide MCP or secrets here. If this folder is embedded in a larger workspace, also respect the parent project’s agent docs when they conflict on paths or global policies.
